#include <bits/stdc++.h>

using namespace std;

int main()
{
    string str;
    cin >> str;
    int numberOfWord = 1;
    for (char ch: str)
    {
        if (ch >= 'A' and ch <= 'Z') numberOfWord++;
    }
    cout << numberOfWord;
    return 0;
}
